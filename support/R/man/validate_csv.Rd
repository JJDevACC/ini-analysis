% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{validate_csv}
\alias{validate_csv}
\title{Validate an edited PI tag master list (Step 1b)}
\usage{
validate_csv(infile = "out/pi_tag_master_list.csv")
}
\arguments{
\item{infile}{Path to the CSV to validate.
Defaults to \code{"data/pi_tag_master_list.csv"}.}
}
\value{
A data frame of rows still needing correction (return value of
\code{\link[=check_tags]{check_tags()}}), invisibly. The tag-classification tree is printed as a
side-effect.
}
\description{
Reads \code{data/pi_tag_master_list.csv} (or a user-supplied path), classifies
each tag, and validates against the PI Web API via \code{\link[=check_tags]{check_tags()}}. Use
this after manually editing the CSV produced by \code{\link[=validate_gis]{validate_gis()}} to confirm
that your corrections are recognised by PI.
}
\seealso{
\code{\link[=validate_gis]{validate_gis()}} to generate the initial master list,
\code{\link[=build_af]{build_af()}} for the next pipeline step.
}
