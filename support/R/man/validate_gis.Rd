% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{validate_gis}
\alias{validate_gis}
\title{Build PI tag master list from GIS data}
\usage{
validate_gis(
  outfile = "out/pi_tag_master_list.csv",
  verbose = TRUE,
  log_path = NULL
)
}
\arguments{
\item{verbose}{Logical. If \code{TRUE} (default), prints export summary,
duplicate counts, and a data glimpse to the console.}

\item{log_path}{Character. Optional path to a log file. If provided, the
export summary is appended to the file with a timestamp.}
}
\value{
A named list (invisibly) with elements:
\describe{
\item{export}{The exported data frame.}
\item{n_rows}{Number of rows exported.}
\item{outfile}{Path the CSV was written to.}
\item{dup_asset_ids}{Data frame of duplicate \code{CMMS_ASSET_ID} counts.}
\item{dup_pi_tags}{Data frame of duplicate \code{PI_TAG} counts.}
}
}
\description{
Main pipeline entry point (Step 1). Loads lift station data from GIS,
validates tags, guesses missing tag names, and exports to CSV
}
\details{
Pipeline steps:
\enumerate{
\item Load GIS lift station data via \code{\link[=get_ls_from_gis]{get_ls_from_gis()}}
\item Classify and validate tags via \code{\link[=check_tags]{check_tags()}}
\item Guess missing/bad tag names via \code{\link[=guess_tag_name_from_df]{guess_tag_name_from_df()}}
\item Join corrected tags with original GIS data
\item Filter to valid lift stations
(i.e. \code{CMMS_ASSET_ID} starting with \code{"LS"} or \code{"BPS"})
\item Export to \code{data/pi_tag_master_list.csv} OR
user-defined path (\code{outfile} argument)
}

Prints warnings for duplicate \code{CMMS_ASSET_ID}s and \code{PI_TAG}s.
}
\seealso{
\code{vignette("usage-guide")} for step-by-step instructions,
\code{vignette("technical-reference")} for prerequisites and data file
requirements.
}
