## 2026-04-10 Prompt for Initial Steps of the Project
I need you to implement a solution to the project and notes in the "## [I&I] Flow Data to ADS Sliicer Automation" section of C:\repository\local\ini-analysis\human-notes.md.  We are ONLY working on this section right now.  I typed up some notes what I think is an starting point for the process to accomplish in the "### Proposed Process In Conjunction with AI/Kiro" section of the markdown file.  I want to reiterate that I want this process to be simple steps that gradually build on one another until the final goal is reached.  The final goal being an automated system that would post the data from our AVEVA PI system into ADS Prism Sliicer using the API Endpoint noted in the notes.  However, if this is not possible, we will need to evaluate other methods of automation.  Ask me any clarifying questions you may need before proceeding.  Also make any markdown files needed for your own notes on this project.

### Response to Clarifying Questions
Here are responses to your questions:
1.  We need to plan for using the service account credentials from the .env file.  Ultimately, if we're able to get this running in AWS as a Lambda function it would not be authenticating using kerberos, I believe.
2.  Yes, in most cases the pattern follows what you stated. I'm not certain on always using the tag name part as the "site ID", however, let's use that going forward until further notice.  So for now, we would use the part "WES8617" as the sideID from your example.  WE can modify this later if needed.
3.   I do not know the answer to this question.  Let's assume that even though the R code is averaging that this is something that needs to be in the CSV file for Sliicer and not related to how we get he data.  Also, make a "ai-questions.md" markdown file in the C:\repository\local\ini-analysis\data-to-sliicer folder and add this as a clarifying question we will eventually want to resolve.
4.  I do not know the answer to this question, but I noticed that as well.  For now, let's assume that all three columns will always be the same value.  Add this to the "ai-questions.md" file for us to inquire about later on.
5.  I have an API key that we should be able to use when we get to that point.  The endpoint from the swagger spec that I believe we would use is POST to /api/Telemtry.  However, this hasn't been confirmed.  Let's worry more about this section as we move through the project.  
6.  Yes, the .venv folder that is in this project is what we should use.  It has Python 3.12 and other libraries already installed.

### 2026-04-13 Prompt about finding the difference between the two API Calls.
First, to answer your question, yes add this to ai-question.md so we remember to get clarification on it from Craig.  Second, I think it would be helpful to map out the original, exact, REST API Calls that both Craig's R code and our Python code makes so that I can check if the API endpoint that's used is the same in both cases, and determine if some of the R code client side manipulation that Craig did is what's causing the difference in the flow values.  For example, the idea is that if the R code and our Python code make the same API call and the values that are received are the same in R and Python, then I should be able to determine where the difference is.  I would like to test the API endpoints that are used in both cases using Postman so I can conduct simple side-by-side comparisons to understand what's going on.